//Megodos para las mayusculas y minusculas 
const producto = 'Monitor 20 Pulgadas';
//Si quetemos pasar esto a mayusculas utilizaremos el metodo toUpperCase con esto lograremos pasar toda la cadena de texto 
console.log(producto.toUpperCase());

console.log(producto.toLowerCase());


const email ="CORREO@CORREO.COM";
//Si quetemos pasar esto a minuscolas utilizaremos el metodo toLoweCase con esto lograremos pasar toda la cadena de texto 
console.log(email.toLowerCase());

const presio =300;

console.log(presio);
//Los metodos que contienen numeros y son convertidos a String son diferentes a como si los pasamos como numero 
console.log(presio.toString());